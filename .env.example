# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/toxicity_shield

# Redis
REDIS_URL=redis://localhost:6379

# ML Service
ML_SERVICE_URL=http://localhost:8000

# App URL (required for OAuth - must be publicly accessible)
# PUBLIC_APP_URL=https://your-domain.com

# Session encryption (32-byte key, base64 encoded)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=

# Email (Resend)
# RESEND_API_KEY=
